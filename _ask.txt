Q1 发现真实 bug：在 /user/<别人的 id> 页面，panel 错拿被访问那人的昵称/头像/等级。
（在默认 Default 模式，问不出多选，等你回话。）

三个选择：
A. 现在修（推荐）：在 readFromDom 里检测"是否在别人个人页"，是的话只信 nav-mine + 头像兜底；先写测试、再修、跑通再推 GF。Q2/Q3 调研照常做。
B. 先做完 Q1/Q2/Q3 三份调研再统一修。
C. 只调研不修，不推 GF（本地 1.1.4 装在你 Chrome 上给你自己看）。

你的选择？
